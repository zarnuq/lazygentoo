early_microcode=yes
# GPU firmware is intentionally NOT bundled: no early KMS / plymouth here
# (plain-text LUKS prompt on the EFI framebuffer), so the GPU driver loads
# its firmware from the real rootfs after switch_root. Keeps the UKI lean.
