permit persist :wheel
permit nopass :wheel cmd /usr/local/sbin/poweroff
permit nopass :wheel cmd /usr/local/sbin/reboot
permit nopass :wheel cmd /usr/local/sbin/halt
permit nopass :wheel cmd /usr/local/sbin/shutdown
